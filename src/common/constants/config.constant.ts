export const PAGINATION_DEFAULTS = {
  DEFAULT_OFFSET_MULTIPLIER: 1, // For (page - 1) * limit
};

export const CHAT_CONFIG = {
  CONVERSATION_PARTICIPANT_COUNT: 2,
  MESSAGE_QUERY_LIMIT: 1, // For LIMIT 1 in lastMessage subquery
};
